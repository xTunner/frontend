mongo
        mongo --version
redis
        redis-cli --version

mysql
        mysql --version

postgres
        psql --version

memcached
        memcached -h | head -1

sphinx
        indexer --version | head -1

riak
        riak version



rvm
        rvm info ruby
        bundle --version


General
        env
        git --version
        cc --version | head -1
        c++ --version | head -1
        make --version | head -1


php
        php5 --version

node
        node --version
        npm --version

clojure
        lein --version
        java -version

python
        python --version
