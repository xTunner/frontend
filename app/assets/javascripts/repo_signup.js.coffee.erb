
# Replace ASAP with the latest content.
# poller = (retry) ->
#     $.get(<%= "/add-repo/content" =>,
#           (data, textStatus) ->
#                             else if data
#                                 alert("success")
#                                 # TODO: fix redirections
#                             else
#                                 alert("backward")
#                                 history.back()
#           )




# $(document).ready ->
#   $.smartPoller(10, poller)
