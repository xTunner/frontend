Commit <%= @build.vcs_revision %> timed out during testing, after 5 minutes without output.

<%= render "full_build_report" %>

<%= render "last_command" %>

<%= render "rest_of_commands" %>

<%= render "full_build_report" %>
