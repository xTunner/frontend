;;; If it has package.json, assume it uses npm.
                                        ; npm uses `npm test`
                                        ; otherwise `make test`
;;; install using `npm install`

;;; changed in version 0.6 to install npm by default
