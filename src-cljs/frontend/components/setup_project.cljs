(ns frontend.components.setup-project
  (:require [frontend.utils :as utils :refer-macros [defrender html]]
            [om.core :as om :include-macros true]))

(defrender setup-project [data owner]
  (html [:div "Placeholder for onboarding workflow (yml setup, etc.)"]))
